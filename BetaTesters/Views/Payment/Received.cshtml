@model IEnumerable<PaymentViewModel>

@{
    ViewBag.Title = "Received payments";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />

@if (Model.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th><label>Sender</label></th>
                <th><label>Amount</label></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var payment in Model)
            {
                <tr>
                    <td><label>@($"{payment.Sender.FirstName} {payment.Sender.LastName}")</label></td>
                    <td><label>@String.Format("{0:f2}", payment.Amount)</label></td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <h3 class="text-center">Your currently have no received payments!</h3>
}


